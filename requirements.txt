# Core modelling stack. The graph-attention layers are implemented directly in
# modelTraining/model.py, so torch-geometric is not required.
torch>=2.1
torchvision>=0.16
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8

# Vision and feature extraction
opencv-python>=4.9
pillow>=10.0
mediapipe>=0.10        # face landmarks and blendshapes
tqdm>=4.66

# Audio and speech
librosa>=0.10
soundfile>=0.12
openai-whisper>=20231117

# ROS bag reading (dataExtraction/facial_keypoints_extraction)
rosbags>=0.9

# Robot integration (modelTraining/furhat_*.py). Install on the machine hosting
# the Furhat SDK; the Realtime API answers keyless auth for local clients only.
# furhat_realtime_api
